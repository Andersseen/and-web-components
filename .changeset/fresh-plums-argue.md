---
'@andersseen/skills': patch
---

Documents the new Tailwind-optional styling paths (`tokens.css`, `elements.css`,
`tailwind-preset`) and clarifies that `and-theme` now has a full structural
effect (navbar/sidebar/carousel dimensions, motion, focus ring), not just
radius/spacing, plus the custom-theme override recipe.
