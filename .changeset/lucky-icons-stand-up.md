---
'@andersseen/icon': patch
---

Fix the `lock` and `user` icons, which were each missing half their artwork.

`lock` was a bare rounded rectangle with no shackle, so it rendered as an
unrecognisable box, and `user` was only the shoulders arc with no head. Both now
carry the full two-shape outline the rest of the set uses.
