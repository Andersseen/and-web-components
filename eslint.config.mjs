/**
 * Root ESLint Configuration
 *
 * This is a stub configuration. Each package has its own eslint.config.mjs
 * that uses its own tsconfig.json. Run 'pnpm lint' from root to lint all packages.
 *
 * For linting individual packages:
 *   pnpm -C packages/headless-core lint
 *   pnpm -C packages/web-components lint
 */

// This file exists for IDE support but actual linting is done per-package
export default [];
