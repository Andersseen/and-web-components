import { enableAnimations } from '@andersseen/web-components';

/**
 * Enables animations for all @andersseen/web-components.
 * Call once at app startup, similar to `initializeIcons()`.
 */
export const initializeAnimations = () => {
  enableAnimations();
};
