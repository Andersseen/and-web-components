{
  "name": "and-web-components",
  "private": false,
  "scripts": {
    "build:headless": "pnpm -C packages/headless-core build",
    "build:motion": "pnpm -C packages/motion-core build",
    "build:stencil": "pnpm build:headless && pnpm -C packages/stencil-library build",
    "start:demo": "pnpm build:stencil && pnpm build:motion && pnpm -C packages/angular-workspace start",
    "build:demo": "pnpm build:stencil && pnpm build:motion && pnpm -C packages/angular-workspace build demo-app",
    "deploy:cloudflare": "pnpm build:demo && pnpm -C packages/angular-workspace exec wrangler pages deploy"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "lerna": "^9.0.3",
    "typescript": "^5.9.3"
  }
}
