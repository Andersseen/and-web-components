{
  "name": "and-web-components",
  "private": false,
  "scripts": {
    "build:stencil": "pnpm -C packages/stencil-library build",
    "start:demo": "pnpm -C packages/stencil-library build && pnpm -C packages/angular-workspace start",
    "build:demo": "pnpm build:stencil && pnpm -C packages/angular-workspace build demo-app",
    "deploy:cloudflare": "pnpm build:demo && pnpm -C packages/angular-workspace exec wrangler pages deploy"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "lerna": "^9.0.3",
    "typescript": "^5.9.3"
  }
}
